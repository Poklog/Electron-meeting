# Copy to .env (optional) and adjust as needed.

# Supabase PostgreSQL Connection
# 從 Supabase 專案設定中取得以下資訊：
#
# 步驟 1: 登入 Supabase Dashboard
# 步驟 2: 進入 Project Settings → Database
# 步驟 3: 複製連接字串或手動構建
#
# 格式: postgresql+psycopg://postgres:[PASSWORD]@[HOST]:5432/postgres
# 取代部分:
#   [PASSWORD] = 建立專案時設定的資料庫密碼
#   [HOST]     = 你的 Supabase 專案 ID + .supabase.co
#
# 完整範例：
# DATABASE_URL=postgresql+psycopg://postgres:mypa55w0rd@abcdefgh.supabase.co:5432/postgres

DATABASE_URL=

# 選項：如需自動構建連接（需手動配置完整 DATABASE_URL）
SUPABASE_URL=
SUPABASE_ANON_KEY=

# Gemini API 設定
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
GEMINI_ENDPOINT=https://generativelanguage.googleapis.com/v1beta/models

# 前端 CORS 設定（用於本地開發）
CORS_ORIGINS=http://localhost:5173

# JWT 認證設定
# 注意：請在 backend/.env 中設定一個強大的隨機值（建議32字元以上）
AUTH_SECRET_KEY=
AUTH_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7
